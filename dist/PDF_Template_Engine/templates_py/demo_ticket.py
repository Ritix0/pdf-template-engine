# templates_py/demo_ticket.py

# Базовые настройки отображения
CHAR_ON = "V"
CHAR_OFF = ""
CHAR_RESTRICTED_ON = "X"
CHAR_RESTRICTED_OFF = ""

# Настройки даты
DATE_FORMAT = "word"      # "word" - месяц прописью, "numeric" - цифрами
ADD_YEAR_SUFFIX = True    # Добавлять "г." в конце года
QUOTE_DAY = True          # Рисовать кавычки вокруг дня: «01»
UPPERCASE_ALL = True      # Переводить ФИО в верхний регистр (КАПС)

# Секции для удаления старого содержимого (Редактирование)
# Координаты: [x0, y0, x1, y1]
REDACT_BOXES = {
    "clear_header": [[50, 50, 550, 150]],
    "clear_body": [[50, 160, 550, 400]],
    "clear_footer": [[50, 800, 550, 850]]
}

# Координаты для вставки новых данных
# Ключи должны совпадать со значениями из секции "map" в ui_config.json
INSERT_BOXES = {
    # Основные поля
    "event_name": [[100, 100, 500, 130]],
    "client_surname": [[100, 150, 250, 170]],
    "client_name": [[260, 150, 450, 170]],
    
    # QR-код (обрабатывается отдельно в движке)
    "qr_code_image": [[450, 50, 550, 150]],
    
    # Суммы (движок ищет ключи amount_int / amount_dec)
    "amount_int": [[100, 300, 200, 320]],
    "amount_dec": [[210, 300, 250, 320]],
    "amount_full": [[100, 330, 300, 350]],
    
    # Даты (генерируются автоматически в gui.py)
    "date_1": [[100, 400, 130, 420]], # День
    "date_2": [[140, 400, 250, 420]], # Месяц
    "date_3": [[260, 400, 320, 420]], # Год
    
    # Место и площадка
    "venue": [[100, 200, 300, 220]],
    "seat": [[310, 200, 450, 220]],

    # Чекбоксы (для примера)
    "delivery_el": [[50, 500, 70, 520]],
    "delivery_cur": [[50, 530, 70, 550]]
}

# Дополнительные переменные (если нужно)
EXTRA_VARS = {}

# Стили оформления
# ВАЖНО: font указываем ПРОСТО именем файла, движок сам добавит папку fonts/
STYLES = {
    "default": {
        "font": "arial.ttf", 
        "size": 11,
        "color": (0, 0, 0),
        "align": 0 # 0-left, 1-center, 2-right
    },
    "event_name": {
        "size": 18,
        "align": 1 # По центру
    },
    "amount_int": {
        "size": 14,
        "align": 2 # По правому краю
    },
    "amount_dec": {
        "size": 10
    }
}